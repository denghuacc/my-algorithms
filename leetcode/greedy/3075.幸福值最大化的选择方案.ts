/*
 * @lc app=leetcode.cn id=3075 lang=typescript
 *
 * [3075] 幸福值最大化的选择方案
 *
 * https://leetcode.cn/problems/maximize-happiness-of-selected-children/description/
 *
 * algorithms
 * Medium (54.91%)
 * Likes:    686
 * Dislikes: 93
 * Total Accepted:    187.9K
 * Total Submissions: 339.9K
 * Testcase Example:  '[1,2,3]\n2'
 *
 * 给你一个长度为 n 的数组 happiness 和一个正整数 k。
 *
 * 有 n 个孩子排成一行，第 i 个孩子的幸福值为 happiness[i]。你需要分 k 轮
 * 选择 k 个孩子。
 *
 * 每一轮选择一个孩子后，所有还未被选择的孩子幸福值都减少 1。注意幸福值
 * 不能变成负数，只有在当前为正数时才会减少。
 *
 * 返回选择 k 个孩子所能获得的最大幸福值之和。
 *
 *
 * 示例 1：
 *
 *
 * 输入：happiness = [1,2,3], k = 2
 * 输出：4
 * 解释：可以这样选择：
 * - 先选幸福值为 3 的孩子，剩余幸福值变为 [0,1]。
 * - 再选幸福值为 1 的孩子，剩余幸福值变为 [0]。
 * 幸福值之和为 3 + 1 = 4。
 *
 *
 * 示例 2：
 *
 *
 * 输入：happiness = [1,1,1,1], k = 2
 * 输出：1
 * 解释：可以这样选择：
 * - 先选幸福值为 1 的任意孩子，剩余幸福值变为 [0,0,0]。
 * - 再选幸福值为 0 的孩子，剩余幸福值变为 [0,0]。
 * 幸福值之和为 1 + 0 = 1。
 *
 *
 * 示例 3：
 *
 *
 * 输入：happiness = [2,3,4,5], k = 1
 * 输出：5
 * 解释：只选幸福值为 5 的孩子，总和为 5。
 *
 *
 *
 * 提示：
 *
 *
 * 1 <= n == happiness.length <= 2 * 10^5
 * 1 <= happiness[i] <= 10^8
 * 1 <= k <= n
 *
 *
 */

// @lc code=start
/**
 * 选择 k 个孩子，使幸福值总和最大。
 *
 * @param happiness - 每个孩子的幸福值
 * @param k - 选择的孩子数量
 * @returns 最大幸福值总和
 */
function maximumHappinessSum(happiness: number[], k: number): number {
  // 将幸福值按从大到小排序，优先选择幸福值更大的孩子
  happiness.sort((a, b) => b - a);
  let total = 0;
  for (let i = 0; i < k; i++) {
    // 第 i 次选择时，未被选中的孩子都会被减 1，因此实际贡献为 happiness[i] - i
    const current = happiness[i] - i;
    // 幸福值不能为负数，负值按 0 计算
    total += Math.max(0, current);
  }
  return total;
}
// @lc code=end

/*
解题思路详解：

1. 题目理解
   - 问题本质：每次选择会让后续可选值递减，目标是最大化总和。
   - 关键特点：幸福值递减与选择次数相关，且不低于 0。
   - 目标：选 k 个孩子，使总幸福值最大。

2. 解题思路
   核心思想
   - 贪心：优先选择初始幸福值更大的孩子。
   - 第 i 次选择时，全局已发生 i 次递减，因此该孩子贡献为 happiness[i] - i。

   算法步骤
   1) 将 happiness 降序排序。
   2) 遍历前 k 个孩子，第 i 个的有效贡献为 max(0, happiness[i] - i)。
   3) 累加贡献得到答案。

3. 代码实现
   实现步骤
   1) 排序后按顺序选择前 k 个。
   2) 用循环索引 i 直接表示已进行的递减次数。
   3) 对每个贡献进行非负截断。

   关键函数说明
   - maximumHappinessSum：主函数，排序后进行贪心累加。

4. 复杂度分析
   - 时间复杂度：O(n log n)，排序占主导。
   - 空间复杂度：O(1) 额外空间，排序就地完成。
   - 关键观察：递减次数仅与选择轮数相关，与具体选择顺序一致。

5. 示例分析
   示例一：happiness = [1,2,3], k = 2
   - 排序后 [3,2,1]
   - 贡献为 3 和 2-1=1，总和 4。

   示例二：happiness = [1,1,1,1], k = 2
   - 排序后 [1,1,1,1]
   - 贡献为 1 和 1-1=0，总和 1。

   示例三：happiness = [2,3,4,5], k = 1
   - 排序后 [5,4,3,2]
   - 贡献为 5，总和 5。

   边界情况
   - k = 1：直接取最大幸福值。
   - 幸福值递减后为负数时按 0 处理。

6. 算法要点总结
   核心技巧
   - 选择顺序仅影响递减次数，最优策略是排序后取前 k。
   - 使用 i 表示递减轮次，避免额外变量。

   优化要点
   - 循环中直接计算贡献，代码更简洁。
   - 不需要模拟队列，只需排序即可。

   类似问题
   - 选择前 k 个并按轮次递减的贪心问题。
   - “每次操作导致后续收益递减”的最大和问题。

7. 常见错误
   - 忘记对幸福值做非负截断，导致总和偏小。
   - 选择顺序未排序，导致非最优。
   - 使用 k 次循环但未按 i 计算递减次数。
*/
