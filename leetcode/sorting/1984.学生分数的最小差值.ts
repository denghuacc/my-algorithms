/*
 * @lc app=leetcode.cn id=1984 lang=typescript
 *
 * [1984] 学生分数的最小差值
 *
 * https://leetcode-cn.com/problems/minimum-difference-between-highest-and-lowest-of-k-scores/description/
 *
 * algorithms
 * Easy (57.50%)
 * Likes:    27
 * Dislikes: 0
 * Total Accepted:    13.9K
 * Total Submissions: 22.2K
 * Testcase Example:  '[90]\n1'
 *
 * 给你一个 下标从 0 开始 的整数数组 nums ，其中 nums[i] 表示第 i 名学生的分数。另给你一个整数 k 。
 *
 * 从数组中选出任意 k 名学生的分数，使这 k 个分数间 最高分 和 最低分 的 差值 达到 最小化 。
 *
 * 返回可能的 最小差值 。
 *
 *
 *
 * 示例 1：
 *
 * 输入：nums = [90], k = 1
 * 输出：0
 * 解释：选出 1 名学生的分数，仅有 1 种方法：
 * - [90] 最高分和最低分之间的差值是 90 - 90 = 0
 * 可能的最小差值是 0
 *
 *
 * 示例 2：
 *
 * 输入：nums = [9,4,1,7], k = 2
 * 输出：2
 * 解释：选出 2 名学生的分数，有 6 种方法：
 * - [9,4,1,7] 最高分和最低分之间的差值是 9 - 4 = 5
 * - [9,4,1,7] 最高分和最低分之间的差值是 9 - 1 = 8
 * - [9,4,1,7] 最高分和最低分之间的差值是 9 - 7 = 2
 * - [9,4,1,7] 最高分和最低分之间的差值是 4 - 1 = 3
 * - [9,4,1,7] 最高分和最低分之间的差值是 7 - 4 = 3
 * - [9,4,1,7] 最高分和最低分之间的差值是 7 - 1 = 6
 * 可能的最小差值是 2
 *
 *
 *
 * 提示：
 *
 *
 * 1 <= k <= nums.length <= 1000
 * 0 <= nums[i] <= 10^5
 *
 *
 */

// @lc code=start
// sorting
/**
 * 选择 k 个学生分数，使最高分与最低分的差值最小。
 *
 * @param nums - 学生分数数组
 * @param k - 选择的学生人数
 * @returns 最小可能的差值
 */
function minimumDifference(nums: number[], k: number): number {
  if (k === 1) {
    // 只有一个分数时，最高分与最低分相等
    return 0;
  }
  let res = Infinity;
  const n = nums.length;
  // 排序后，任意 k 个连续元素的差值是该窗口内最大与最小的差
  nums.sort((a, b) => a - b);
  for (let i = 0; i + k - 1 < n; i++) {
    // 计算长度为 k 的窗口差值，取最小
    res = Math.min(res, nums[i + k - 1] - nums[i]);
  }
  return res;
}
// @lc code=end

/*
解题思路详解：

1. 题目理解
   - 问题本质：从分数数组中选 k 个，使最大值与最小值差最小。
   - 关键特点：只关心最大值与最小值，中间元素不影响差值。
   - 目标：最小化差值。

2. 解题思路
   核心思想
   - 排序后，任意 k 个元素的最小差值一定来自连续区间。
   - 因为非连续选择会扩大最大值与最小值的跨度。

   算法步骤
   1) 若 k == 1，直接返回 0。
   2) 将 nums 升序排序。
   3) 用滑动窗口遍历所有长度为 k 的连续区间。
   4) 计算每个区间的差值并取最小。

3. 代码实现
   实现步骤
   - 排序后用索引 i 与 i+k-1 确定窗口两端。
   - 比较所有窗口差值的最小值。

   关键函数说明
   - minimumDifference：主函数，排序并扫描窗口差值。

4. 复杂度分析
   - 时间复杂度：O(n log n)，排序为主。
   - 空间复杂度：O(1) 额外空间（排序就地）。
   - 关键观察：最小差值来自连续 k 个元素。

5. 示例分析
   示例一：nums = [90], k = 1
   - 只选一个，差值为 0。

   示例二：nums = [9,4,1,7], k = 2
   - 排序后 [1,4,7,9]。
   - 窗口差值为 3、3、2，最小为 2。

   边界情况
   - k 等于数组长度，差值为最大值减最小值。
   - 数组元素相同，差值为 0。

6. 算法要点总结
   核心技巧
   - 排序后用固定窗口比较最大与最小即可。
   - 连续区间保证差值最小化。

   优化要点
   - 仅需一次扫描窗口，逻辑简单高效。
   - 无需复杂数据结构。

   类似问题
   - 最小跨度选择问题。
   - 排序 + 固定窗口最优化问题。

7. 常见错误
   - 未排序直接选 k 个，导致非最优。
   - 窗口索引越界，遗漏最后一个区间。
   - 忘记 k=1 的特判。
*/
