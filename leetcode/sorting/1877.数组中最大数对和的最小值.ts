/*
 * @lc app=leetcode.cn id=1877 lang=typescript
 *
 * [1877] 数组中最大数对和的最小值
 *
 * https://leetcode-cn.com/problems/minimize-maximum-pair-sum-in-array/description/
 *
 * algorithms
 * Medium (81.38%)
 * Likes:    28
 * Dislikes: 0
 * Total Accepted:    11.6K
 * Total Submissions: 13.8K
 * Testcase Example:  '[3,5,2,3]'
 *
 * 一个数对 (a,b) 的 数对和 等于 a + b 。最大数对和 是一个数对数组中最大的 数对和 。
 *
 *
 * 比方说，如果我们有数对 (1,5) ，(2,3) 和 (4,4)，最大数对和 为 max(1+5, 2+3, 4+4) = max(6, 5, 8) =
 * 8 。
 *
 *
 * 给你一个长度为 偶数 n 的数组 nums ，请你将 nums 中的元素分成 n / 2 个数对，使得：
 *
 *
 * nums 中每个元素 恰好 在 一个 数对中，且
 * 最大数对和 的值 最小 。
 *
 *
 * 请你在最优数对划分的方案下，返回最小的 最大数对和 。
 *
 *
 *
 * 示例 1：
 *
 * 输入：nums = [3,5,2,3]
 * 输出：7
 * 解释：数组中的元素可以分为数对 (3,3) 和 (5,2) 。
 * 最大数对和为 max(3+3, 5+2) = max(6, 7) = 7 。
 *
 *
 * 示例 2：
 *
 * 输入：nums = [3,5,4,2,4,6]
 * 输出：8
 * 解释：数组中的元素可以分为数对 (3,5)，(4,4) 和 (6,2) 。
 * 最大数对和为 max(3+5, 4+4, 6+2) = max(8, 8, 8) = 8 。
 *
 *
 *
 *
 * 提示：
 *
 *
 * n == nums.length
 * 2 <= n <= 10^5
 * n 是 偶数 。
 * 1 <= nums[i] <= 10^5
 *
 *
 */

// @lc code=start
// sort
/**
 * 将数组配对，使最大数对和最小。
 *
 * @param nums - 偶数长度的正整数数组
 * @returns 最小化后的最大数对和
 */
function minPairSum(nums: number[]): number {
  const n = nums.length;
  // 升序排序后，最优策略是最小值与最大值配对
  nums.sort((a, b) => a - b);
  let maxPairSum = 0;
  for (let i = 0; i < Math.floor(n / 2); i++) {
    const pairSum = nums[i] + nums[n - 1 - i];
    // 记录所有配对中的最大值
    maxPairSum = Math.max(maxPairSum, pairSum);
  }
  return maxPairSum;
}
// @lc code=end

/*
解题思路详解：

1. 题目理解
   - 问题本质：把数组分成 n/2 对，使“最大数对和”尽可能小。
   - 关键特点：每个元素必须恰好参与一次配对。
   - 目标：最小化所有配对和中的最大值。

2. 解题思路
   核心思想
   - 贪心配对：最小值与最大值配对，可压低最大的配对和。
   - 若让最大值与更大的元素配对，会使最大数对和变大。

   算法步骤
   1) 将 nums 升序排序。
   2) 使用双指针，从两端向中间配对。
   3) 记录所有配对和中的最大值。
   4) 返回该最大值。

3. 代码实现
   实现步骤
   - 排序后用 i 与 n-1-i 形成配对。
   - 每次更新 maxPairSum。

   关键函数说明
   - minPairSum：主函数，执行排序与配对。

4. 复杂度分析
   - 时间复杂度：O(n log n)，排序为主。
   - 空间复杂度：O(1) 额外空间（排序就地）。
   - 关键观察：排序后两端配对保证最大和最小化。

5. 示例分析
   示例一：nums = [3,5,2,3]
   - 排序后 [2,3,3,5]，配对 (2,5) 和 (3,3)。
   - 最大数对和为 7。

   示例二：nums = [3,5,4,2,4,6]
   - 排序后 [2,3,4,4,5,6]。
   - 配对 (2,6),(3,5),(4,4)，最大数对和为 8。

   边界情况
   - 数组长度为 2，结果即为两数之和。
   - 所有数相等，最大数对和为任意一对的和。

6. 算法要点总结
   核心技巧
   - 两端配对使“最大数对和”的上界最小化。
   - 排序后一次扫描即可得到答案。

   优化要点
   - 使用单次循环统计最大配对和。
   - 不需要额外数组或复杂结构。

   类似问题
   - 最小化配对最大值的贪心配对问题。
   - 需要两端配对平衡极值的数组题。

7. 常见错误
   - 误用相邻元素配对，导致最大值偏大。
   - 忘记排序直接配对，无法保证最优。
   - 未更新最大配对和，返回错误结果。
*/
