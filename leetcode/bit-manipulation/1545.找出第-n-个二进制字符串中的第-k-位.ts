/*
 * @lc app=leetcode.cn id=1545 lang=typescript
 *
 * [1545] 找出第 N 个二进制字符串中的第 K 位
 *
 * https://leetcode.cn/problems/find-kth-bit-in-nth-binary-string/description/
 *
 * algorithms
 * Medium (60.24%)
 * Likes:    81
 * Dislikes: 0
 * Total Accepted:    21.5K
 * Total Submissions: 34.7K
 * Testcase Example:  '3\n1'
 *
 * 给你两个正整数 n 和 k，二进制字符串  Sn 的形成规则如下：
 *
 *
 * S1 = "0"
 * 当 i > 1 时，Si = Si-1 + "1" + reverse(invert(Si-1))
 *
 *
 * 其中 + 表示串联操作，reverse(x) 返回反转 x 后得到的字符串，而 invert(x) 则会翻转 x 中的每一位（0 变为 1，而 1 变为
 * 0）。
 *
 * 例如，符合上述描述的序列的前 4 个字符串依次是：
 *
 *
 * S1 = "0"
 * S2 = "011"
 * S3 = "0111001"
 * S4 = "011100110110001"
 *
 *
 * 请你返回  Sn 的 第 k 位字符 ，题目数据保证 k 一定在 Sn 长度范围以内。
 *
 *
 *
 * 示例 1：
 *
 *
 * 输入：n = 3, k = 1
 * 输出："0"
 * 解释：S3 为 "0111001"，其第 1 位为 "0" 。
 *
 *
 * 示例 2：
 *
 *
 * 输入：n = 4, k = 11
 * 输出："1"
 * 解释：S4 为 "011100110110001"，其第 11 位为 "1" 。
 *
 *
 * 示例 3：
 *
 *
 * 输入：n = 1, k = 1
 * 输出："0"
 *
 *
 * 示例 4：
 *
 *
 * 输入：n = 2, k = 3
 * 输出："1"
 *
 *
 *
 *
 * 提示：
 *
 *
 * 1
 * 1
 *
 *
 */

// @lc code=start
/**
 * 找出第 n 个二进制字符串中的第 k 位
 *
 * 核心思想：
 * 不显式构造字符串，而是利用递归定义和对称性定位到上一层。
 *
 * @param n - 字符串层级（1 <= n <= 20）
 * @param k - 目标下标（1-based，且保证在 S_n 长度范围内）
 * @returns 第 k 位字符（"0" 或 "1"）
 *
 * 时间复杂度：O(n)
 * 空间复杂度：O(n)（递归栈）
 */
function findKthBit(n: number, k: number): string {
  // 递归基：S1 = "0"
  if (n === 1) {
    return "0";
  }

  // |S_n| = 2^n - 1，中间位置为 2^(n-1)（1-based）
  const mid = 1 << (n - 1);

  // 递推定义里中间位恒为 '1'
  if (k === mid) {
    return "1";

  // 前半段与 S_{n-1} 完全一致，直接递归到上一层
  if (k < mid) {
    return findKthBit(n - 1, k);
  }

  // 后半段 = reverse(invert(S_{n-1}))。
  // 把 k 映射到上一层对称位置 mirroredK，再把结果取反。
  const mirroredK = 2 * mid - k;
  const bit = findKthBit(n - 1, mirroredK);
  return bit === "0" ? "1" : "0";
}
// @lc code=end

/*
解题思路详解：

1. 题目理解
   - 问题本质：根据递推定义的字符串 S_n，返回第 k 位字符。
   - 关键特点：
     1) S_n = S_{n-1} + "1" + reverse(invert(S_{n-1}))。
     2) 字符串长度快速增长，|S_n| = 2^n - 1。
   - 目标：不能暴力构造完整字符串，需按位置直接定位答案。

2. 解题思路

   核心思想：
   - 对于 S_n 的第 k 位，分三类讨论：
     1) k 在中间位：答案直接是 '1'。
     2) k 在左半段：等价于 S_{n-1} 的第 k 位。
     3) k 在右半段：先映射到左半段对称位，再做一次取反。
   - 右半段映射公式：
     mirroredK = 2 * mid - k，mid = 2^(n-1)。

   为什么公式成立：
   - 右半段是 reverse(invert(S_{n-1}))，说明它和 S_{n-1}
     在位置上是“镜像关系”，在值上是“按位翻转关系”。
   - 因此可把右半段位置映射到 S_{n-1} 的对应下标后再取反。

   算法步骤：
   1. 若 n === 1，返回 "0"（递归基）。
   2. 计算中点 mid = 2^(n-1)。
   3. 若 k === mid，返回 "1"。
   4. 若 k < mid，递归求 findKthBit(n - 1, k)。
   5. 若 k > mid，递归求 findKthBit(n - 1, mirroredK) 并取反。

3. 代码实现

   关键实现点：
   - 使用位运算 `1 << (n - 1)` 计算中点，简洁高效。
   - 不构造 S_n，仅递归定位，避免指数级空间与时间开销。
   - 递归深度最多 n（题目范围内非常安全）。

4. 复杂度分析
   - 时间复杂度：O(n)
     每一层只做 O(1) 判断和一次递归，最多递归 n 层。
   - 空间复杂度：O(n)
     主要是递归调用栈深度。
   - 关键观察：每次递归都把问题规模从 n 缩小到 n - 1。

5. 示例分析

   示例 1：n = 3, k = 1
   - S_3 中点为 4，k < 4，转到 S_2 的第 1 位。
   - S_2 中点为 2，k < 2，转到 S_1 的第 1 位。
   - S_1 = "0"，答案是 "0"。

   示例 2：n = 4, k = 11
   - mid = 8，k > 8，在右半段。
   - mirroredK = 2 * 8 - 11 = 5，先求 S_3 的第 5 位再取反。
   - S_3 的 mid = 4，k = 5 > 4，再映射到 mirroredK = 3，
     先求 S_2 的第 3 位并取反。
   - S_2 的 mid = 2，k = 3 > 2，再映射到 mirroredK = 1，
     先求 S_1 的第 1 位并取反。
   - S_1 第 1 位是 "0"。
   - 回溯三次取反：0 -> 1 -> 0 -> 1，最终答案 "1"。

   示例 3：n = 1, k = 1
   - 直接命中递归基，返回 "0"。

   边界情况：
   - k 正好是中点时，立即返回 "1"。
   - k = 1 时，不一定立刻结束，但会逐层落到 n = 1。

6. 算法要点总结
   - 利用递推结构拆分成“左半 + 中点 + 右半”。
   - 右半本质是“镜像 + 取反”，位置和数值都可映射。
   - 不构造字符串，直接按下标递归定位。

7. 常见错误
   - 把中点写错为 `(len / 2)` 后再混用 0-based 与 1-based 下标。
   - 右半段映射公式写错（应为 `2 * mid - k`）。
   - 右半段只做了镜像没做取反，或取反次数不对。
*/
