/*
 * @lc app=leetcode.cn id=3634 lang=typescript
 *
 * [3634] 使数组平衡的最少移除数目
 *
 * https://leetcode.cn/problems/minimum-removals-to-balance-array/description/
 *
 * algorithms
 * Medium (38.12%)
 * Likes:    271
 * Dislikes: 15
 * Total Accepted:    61.1K
 * Total Submissions: 141K
 * Testcase Example:  '[2,1,5]\n2'
 *
 * 给你一个整数数组 nums 和一个整数 k。
 *
 * 若数组的最大值不超过最小值的 k 倍，则称该数组为平衡数组。
 *
 * 你可以删除任意数量的元素，但数组不能为空。
 *
 * 返回使剩余数组平衡所需删除的最少元素个数。
 *
 * 注意：长度为 1 的数组一定是平衡的。
 *
 *
 * 示例 1：
 *
 *
 * 输入：nums = [2,1,5], k = 2
 * 输出：1
 * 解释：删除 5 后得到 [2,1]，满足 2 <= 1 * 2。
 *
 *
 * 示例 2：
 *
 *
 * 输入：nums = [1,6,2,9], k = 3
 * 输出：2
 * 解释：删除 1 和 9 后得到 [6,2]，满足 6 <= 2 * 3。
 *
 *
 * 示例 3：
 *
 *
 * 输入：nums = [4,6], k = 2
 * 输出：0
 * 解释：原数组已满足 6 <= 4 * 2，无需删除。
 *
 *
 * 提示：
 *
 *
 * 1 <= nums.length <= 10^5
 * 1 <= nums[i] <= 10^9
 * 1 <= k <= 10^5
 *
 *
 */

// @lc code=start
/**
 * 返回使数组平衡的最少删除次数。
 *
 * @param nums - 整数数组
 * @param k - 最大值允许的倍数
 * @returns 最少删除次数
 */
function minRemoval(nums: number[], k: number): number {
  const n = nums.length;
  // 排序后只需保留一个连续窗口，使得最大值 <= 最小值 * k
  nums.sort((a, b) => a - b);
  let minRemovals = n;
  let right = 0;
  for (let left = 0; left < n; left++) {
    // 扩展右指针，找到满足条件的最大窗口
    while (right < n && nums[right] <= nums[left] * k) {
      right++;
    }
    // 删除窗口外的元素数量
    minRemovals = Math.min(minRemovals, n - (right - left));
  }
  return minRemovals;
}
// @lc code=end

/*
解题思路详解：

1. 题目理解
   - 问题本质：删除若干元素，使剩余数组最大值 <= 最小值 * k。
   - 关键特点：删除任意元素，但不能删空；单元素一定平衡。
   - 目标：最小化删除数量，等价于最大化保留的元素个数。

2. 解题思路
   核心思想
   - 排序后，若取一个连续子数组作为保留集合，其最小值与最大值分别是两端。
   - 只需找最长的连续窗口，使得 nums[right] <= nums[left] * k。
   - 最少删除 = n - 窗口长度。

   算法步骤
   1) 对 nums 升序排序。
   2) 使用双指针维护窗口 [left, right)：
      - 对每个 left，尽可能向右扩展 right，使条件成立。
   3) 更新最小删除数为 n - (right - left)。
   4) 返回最小删除数。

3. 代码实现
   实现步骤
   - 排序后使用滑动窗口。
   - right 指针单调递增，整体 O(n)。

   关键函数说明
   - minRemoval：主函数，返回最少删除次数。

4. 复杂度分析
   - 时间复杂度：O(n log n)，排序为主，双指针 O(n)。
   - 空间复杂度：O(1) 额外空间（排序就地）。
   - 关键观察：最大化可保留窗口等价于最少删除。

5. 示例分析
   示例一：nums = [2,1,5], k = 2
   - 排序后 [1,2,5]，窗口最大长度为 2（[1,2]）。
   - 删除 1 个元素，答案为 1。

   示例二：nums = [1,6,2,9], k = 3
   - 排序后 [1,2,6,9]，最大窗口长度为 2（[2,6]）。
   - 删除 2 个元素，答案为 2。

   示例三：nums = [4,6], k = 2
   - 排序后 [4,6] 满足 6 <= 4 * 2，删除 0。

   边界情况
   - n = 1：答案为 0。
   - k = 1：要求所有保留元素相等，窗口只含相等值。

6. 算法要点总结
   核心技巧
   - 排序 + 双指针寻找最长满足条件窗口。
   - 通过“最大保留”推导“最少删除”。

   优化要点
   - right 指针单调递增，无需回退。
   - 只需比较窗口两端即可判断条件。

   类似问题
   - 最大满足条件子数组长度问题。
   - 排序后双指针优化范围约束问题。

7. 常见错误
   - 未排序直接滑窗，导致最小值与最大值不正确。
   - 窗口条件用错方向（max <= min * k）。
   - 忘记数组不能为空的约束。
*/
